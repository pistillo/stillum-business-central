# Stillum Business Portal – variabili d'ambiente di esempio
# Copiare in .env e adattare i valori (non committare .env)

# PostgreSQL (docker-compose)
POSTGRES_USER=stillum
POSTGRES_PASSWORD=stillum123
POSTGRES_DB=stillumdb

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Temporal (usa le stesse credenziali PostgreSQL per persistence)
# DB=postgresql, POSTGRES_SEEDS=postgres
