# Quarkus Configuration
quarkus.application.name=stillum-runtime-gateway
quarkus.application.version=1.0.0

# Server Configuration (port 8080 inside container, mapped externally by docker-compose)
quarkus.http.port=8080
quarkus.http.host=0.0.0.0
quarkus.http.cors=true
quarkus.http.cors.origins=/.*/

# Database Configuration
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=stillum
quarkus.datasource.password=stillum123
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/stillum_gateway

quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.enabled=false

# Temporal Configuration
stillum.temporal.server-host=localhost
stillum.temporal.server-port=7233
stillum.temporal.namespace=default

# Workflow Configuration
stillum.workflow.task-queue=stillum-tasks
stillum.workflow.worker-threads=10

# Health Checks
quarkus.smallrye-health.root-path=/health

# Logging
quarkus.log.level=INFO
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
